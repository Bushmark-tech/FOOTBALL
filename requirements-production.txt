# Production Requirements for Scalability
# Install with: pip install -r requirements-production.txt

# Core Django
Django>=4.2.0,<5.0.0

# Database (PostgreSQL for production)
psycopg2-binary>=2.9.9
django-db-geventpool>=4.0.1  # Optional: Connection pooling with gevent

# Caching (Redis)
django-redis>=5.4.0
redis>=5.0.1
hiredis>=2.2.3  # Faster C-based Redis parser

# Web Server
gunicorn>=21.2.0
gevent>=23.9.1  # For async workers

# Background Tasks (Optional but recommended)
celery>=5.3.4
redis>=5.0.1  # Celery broker

# Performance Monitoring
django-debug-toolbar>=4.2.0  # Development only
django-silk>=5.0.4  # Production profiling

# Security
django-csp>=3.7  # Content Security Policy
django-cors-headers>=4.3.1  # CORS handling

# Rate Limiting
django-ratelimit>=4.1.0

# Environment Variables
python-dotenv>=1.0.0

# API Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0

# Data Processing
numpy>=1.26.0
pandas>=2.1.3
joblib>=1.3.2
scikit-learn>=1.3.2

# Utilities
requests>=2.31.0
python-dateutil>=2.8.2

# Static Files (Production)
whitenoise>=6.6.0  # Serve static files efficiently

# Error Tracking (Optional)
# sentry-sdk>=1.38.0  # Uncomment if using Sentry

# Compression
brotli>=1.1.0  # Better compression than gzip

# Health Checks
django-health-check>=3.18.1

# Database Optimization
django-cachalot>=2.6.1  # Automatic caching

